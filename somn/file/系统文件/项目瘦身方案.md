# Somn 项目瘦身方案

生成时间: 2026-04-25T10:36:01.042431
项目路径: somn

## 执行摘要

- **潜在可节省总空间**: 10.41 GB
- **可立即安全清理**: 65.58 MB
- **立即执行操作**: 2 项
- **需要审查操作**: 4 项
- **长期优化建议**: 3 项

## 一、可立即执行的安全操作（低风险）

### 1. 删除所有 __pycache__ 目录和 .pyc/.pyo/.pyd 文件

**风险等级**: 低风险

**说明**: 这些文件是Python字节码缓存，可以在运行时自动重新生成

**可节省空间**: 65.58 MB

**执行命令**:

```bash
powershell -Command "Get-ChildItem -Path 'somn' -Include '__pycache__' -Recurse -Directory | Remove-Item -Recurse -Force"
```

```bash
powershell -Command "Get-ChildItem -Path 'somn' -Include '*.pyc','*.pyo','*.pyd' -Recurse -File | Remove-Item -Force"
```

---

### 2. 删除 138 个空目录

**风险等级**: 低风险

**说明**: 空目录不影响功能，可安全删除

**执行命令**:

```bash
powershell -Command "Get-ChildItem -Path 'somn' -Recurse -Directory | Where-Object { $_.GetFiles().Count -eq 0 -and $_.GetDirectories().Count -eq 0 } | Remove-Item -Force"
```

---

## 二、需要审查的操作（中等/高风险）

### 1. 审查并删除临时/日志文件 (8 个文件)

**风险等级**: 中等风险

**说明**: 需要确认这些文件是否还用途

**可节省空间**: 27.42 MB

---

### 2. 审查 node_modules 目录 (8.05 MB)

**风险等级**: 低风险（可重新生成）

**说明**: 可以通过 npm install 重新生成，但删除后需要重新下载

**可节省空间**: 8.05 MB

**相关命令**:

```bash
npm install
```

---

### 3. 审查 models 目录 (10.31 GB)

**风险等级**: 高风险（重新下载耗时）

**说明**: 模型文件较大，可以考虑使用Git LFS或放到单独的位置

**可节省空间**: 10.31 GB

---

### 4. 审查 data 目录 (486.56 KB)

**风险等级**: 高风险（包含重要数据）

**说明**: 数据目录包含重要数据，需要仔细审查。根据项目规则，部分目录（如 data/memory_v2/, data/learning/ 等）绝对不能删除

**可节省空间**: 486.56 KB

---

## 三、长期优化建议

### 1. 完善 .gitignore 文件

建议添加 3 个忽略规则，防止缓存和临时文件被提交到版本控制

**建议添加的忽略规则**:

- `*.pyc`
- `*.pyo`
- `*.pyd`

---

### 2. 使用 Git LFS 管理大文件

对于 models/ 目录中的模型文件，建议使用 Git LFS 来管理，减少仓库大小

---

### 3. 定期清理缓存和临时文件

建议将缓存清理命令添加到定期任务中，保持项目整洁

---

## 四、大目录分析

共发现 4 个可能的大目录:

- **node_modules** - 8.05 MB (384 文件)
- **models** - 10.31 GB (16 文件)
- **data** - 486.56 KB (784 文件)
- **.workbuddy** - 500.21 KB (107 文件)

## 五、.gitignore 建议

建议将以下 3 个规则添加到 `.gitignore`:

- `*.pyc`
- `*.pyo`
- `*.pyd`

## 六、执行步骤建议

1. **第一步**: 执行「可立即执行的安全操作」，这些操作风险低，可显著减少项目大小
2. **第二步**: 审查「需要审查的操作」，根据实际情况决定是否执行
3. **第三步**: 实施「长期优化建议」，完善项目配置
4. **第四步**: 定期执行缓存清理，保持项目整洁

---

*本方案由项目瘦身方案生成器自动生成*
