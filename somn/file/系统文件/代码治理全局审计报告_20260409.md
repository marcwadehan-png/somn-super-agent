# 代码治理全局审计报告（2026-04-09）

> 本文档整合了代码诊断、修复执行、验证报告、性能诊断的全部内容，形成完整的代码治理闭环记录。
>
> **合并来源**：全局代码诊断详细报告_20260409.md、全局代码诊断报告_20260409.md、全局代码规范修复执行报告.md、全局代码规范修复验证报告_20260409.md、代码治理_修复报告_v1.md、代码治理_项目现状诊断报告.md、代码分析报告_20260408_全局.md、性能诊断报告_20260409.md 及相关历史版本
>
> **整理时间**：2026-04-23

---

## 一、执行摘要

### 1.1 代码资产统计

| 模块 | 文件数 | 代码行数 | 状态 |
|------|--------|----------|------|
| 智慧引擎 (intelligence) | 203 | 95,098 | 规模最大 |
| 神经记忆 (neural_memory) | 55 | 22,906 | 模块复杂 |
| 核心系统 (core) | 34 | 17,688 | 正常 |
| 增长引擎 (growth_engine) | 15 | 11,052 | 正常 |
| 学习系统 (learning) | 19 | 4,490 | 正常 |
| 知识图谱 (knowledge_graph) | 7 | 3,491 | 正常 |
| **总计** | **372** | **171,133** | — |

**全局统计**：项目共 811 个 Python 文件，合计 246,095 行代码。

### 1.2 五项全局代码规范

| 原则 | 要求 |
|-----|------|
| 1 | 只定义只读全局常量/配置，禁止可修改全局变量 |
| 2 | 仅做依赖导入、环境与全局单例资源一次性初始化 |
| 3 | 所有业务逻辑全部封装进函数/类，全局不写业务代码 |
| 4 | 统一设置程序唯一入口，全局仅触发入口执行 |
| 5 | 模块文件仅在全局做接口导出暴露，不做额外逻辑 |

### 1.3 诊断结果汇总（2026-04-09）

| 严重程度 | 数量 | 描述 |
|---------|------|------|
| 🔴 严重 | 2 | 入口点不唯一、模块级引擎实例化 |
| 🟠 高危 | 101 | 多处 `if __name__ == "__main__"` 入口 |
| 🟡 中危 | 19 | 模块级 `logging.basicConfig()` 调用 |
| 🟡 中危 | 15 | 可变类型全局常量（`{}` / `[]`） |
| 🟢 低危 | ~170 | 部分模块缺少 `__all__` 定义 |

---

## 二、违规项详细清单

### 2.1 原则一：可变类型全局常量

**违规文件（15个）**：

| 文件路径 | 变量类型 | 数量 |
|---------|---------|------|
| `persona_core.py` | dict, list | 5 |
| `_smoke_test_memory_writeback.py` | dict, list | 10+ |
| `_smoke_test*.py` | list | 4 |

**修复方案**：使用 `Final[tuple]` 或惰性属性替代可变容器。

### 2.2 原则二：模块级 logging.basicConfig()

**违规文件（19个）**：包括 `src/somn.py`（主入口保留）、`integration/*.py`、`neural_memory/*.py`、`knowledge_graph/*.py` 等。

**修复方案**：子模块仅通过 `logging.getLogger(__name__)` 获取logger，日志配置统一在主入口执行。

### 2.3 原则三：业务逻辑在模块级执行

**典型违规**：

- `src/core/paths.py`：模块级执行目录创建（9个目录在导入时即被创建）
- `src/intelligence/engines/psychology_pioneer_fusion.py`：模块级创建引擎实例

**修复方案**：使用惰性加载函数 `@lru_cache(maxsize=1)`。

### 2.4 原则四：多入口点问题

**违规统计**：发现 **101个文件** 包含 `if __name__ == "__main__"` 代码块。

**入口统一**：保留 `src/somn.py`（主入口）和 `src/__main__.py`（包级入口），其余通过注释禁用。

### 2.5 原则五：缺少 `__all__` 定义

**统计**：有 `__all__` 定义：614 个文件；无 `__all__`：约 230 个（主要是测试文件和内部模块）。

**核心模块覆盖**：✅ `src/core/` ✅ `src/utils/` ✅ `src/intelligence/engines/` 全部覆盖。

---

## 三、代码分析（2026-04-08）

### 3.1 项目规模总览

| 目录 | 行数 | 占比 | 说明 |
|------|------|------|------|
| smart_office_assistant/ | 233,678 | 94.9% | 主项目 |
| ai_core/ | 10,970 | 4.5% | AI核心引擎（独立） |
| 根目录 | 961 | 0.4% | 工具脚本 |

**主项目内部分布（Top 10）**：

| 子目录 | 行数 | 说明 |
|--------|------|------|
| src/intelligence/ | 92,336 | 智慧引擎群（39.5%） |
| src/neural_memory/ | 23,117 | 神经记忆系统 |
| src/core/ | 18,850 | 核心调度链 |
| tests/ | 10,646 | 测试代码 |
| src/growth_engine/ | 10,565 | 增长引擎 |
| scripts/tools/ | 8,338 | 工具脚本 |
| src/ppt/ | 6,636 | PPT生成 |
| src/learning/ | 4,490 | 学习系统 |
| src/knowledge_graph/ | 3,780 | 知识图谱 |
| src/tool_layer/ | 3,465 | 工具层 |

### 3.2 超大文件清单（1000+行，19个）

| 行数 | 文件 | 说明 |
|------|------|------|
| 1771 | src/gui/main_window.py | GUI主窗口 |
| 1543 | scripts/tools/global_code_fixer.py | 代码修复工具（一次性） |
| 1426 | src/core/somn_core.py | 核心系统 |
| 1154 | src/intelligence/engines/cross_scale_thinking_engine.py | 包结构已就绪 |
| 1145 | src/ppt/chart_generator.py | 图表生成器 |
| 1109 | src/growth_engine/.trash_dir/solution_templates_v1.py | 废弃文件 |
| 1089 | src/intelligence/scheduler/_tier3_scheduler.py | Tier3调度器 |
| 1082 | src/somn.py | 主入口模块 |

### 3.3 废代码识别

| 文件 | 问题 |
|------|------|
| `.trash_dir/solution_templates_v1.py` | 已废弃，未引用，应清理 |
| `growth_engine.py` | import不存在的v2模块 |

---

## 四、性能诊断（2026-04-09）

### 4.1 系统启动调用链

```
main.py
  └─ MainWindow.__init__()
       ├─ MemorySystem()           # 同步初始化
       ├─ KnowledgeBase()         # 同步初始化
       ├─ AgentCore()
       │    └─ AgentCore._background_warmup() [daemon]
       │         ├─ _warmup_migrated_modules()
       │         ├─ _warmup_learning_system()
       │         ├─ _warmup_somn_core()
       │         │    └─ SomnCore._background_warmup() [daemon]
       │         │         ├─ _ensure_llm()
       │         │         ├─ _ensure_runtime()
       │         │         ├─ _ensure_layers()
       │         │         └─ _ensure_wisdom_layers()
       │         ├─ _warmup_persona()
       │         ├─ _warmup_emotion_wave()
       │         ├─ _warmup_hybrid_router()
       │         └─ _warmup_rag_engine()
       └─ _setup_menu/_setup_ui/_setup_status_bar/_load_initial_data()
```

### 4.2 五大性能瓶颈

| 瓶颈 | 位置 | 影响 |
|------|------|------|
| 全局顶层导入爆炸 | `src/somn.py`（40+组件） | 导入即触发ChromaDB等初始化 |
| 智慧引擎目录过载 | `src/intelligence/engines/`（121个） | 协调器导入触发大量子模块 |
| ChromaDB初始化 | `memory_system.py` | 耗时2-5秒，主线程执行 |
| MainWindow同步初始化 | `main_window.py` | UI启动阻塞 |
| 首调同步兜底延迟 | `_agent_lifecycle.py` | 后台预热未完成时卡顿 |

### 4.3 优化方案优先级

| 优先级 | 优化项 | 工作量 | 预期效果 | 风险 |
|--------|--------|--------|----------|------|
| P0 | ChromaDB懒加载 | 低 | 启动快3-5秒 | 低 |
| P0 | 延迟导入somn.py | 中 | 启动快2-3秒 | 中 |
| P1 | GUI后台初始化 | 高 | UI响应快 | 中 |
| P1 | 首调优化 | 低 | 减少卡顿 | 低 |
| P2 | 分级预热策略 | 中 | 内存优化 | 低 |

---

## 五、修复执行记录（2026-04-09）

### 5.1 P0-1: logging.basicConfig 统一

- **修复前**：19个文件含模块级日志配置
- **修复后**：仅剩 `src/somn.py`（主入口，正确）
- **状态**：✅ 完成

### 5.2 P0-2: 入口点统一

- **修复前**：101个文件含 `if __name__ == "__main__"`
- **修复后**：保留2个合法入口（`somn.py` + `__main__.py`）
- **测试文件迁移**：
  - `src/learning/test/test_learning_restructure.py` → `tests/`
  - `src/intelligence/engines/test_sharp_engine.py` → `tests/`
- **状态**：✅ 完成

### 5.3 P0-3: 模块级引擎实例化移除

- **修复前**：`psychology_pioneer_fusion.py` 模块级创建实例
- **修复后**：使用惰性加载函数 `@lru_cache(maxsize=1)`
- **状态**：✅ 完成

### 5.4 P1-4: 可变常量重构

- **修复前**：`SHARP_TO_TENDER_BRIDGE = {}`（可变）
- **修复后**：`SHARP_TO_TENDER_BRIDGE: Final[tuple] = ()`
- **状态**：✅ 完成

### 5.5 P1-5: __all__ 覆盖

- **有定义**：614 个文件
- **核心模块覆盖率**：100%
- **状态**：✅ 完成

---

## 六、验证结果（2026-04-09）

### 6.1 验证命令与结果

```bash
# 1. 验证无模块级日志配置（除主入口外）
grep -rn "logging.basicConfig" src/ --include="*.py"
# 结果: 仅 src/somn.py ✅

# 2. 验证入口点唯一
grep -rn "^if __name__ ==" src/ --include="*.py"
# 结果: 2个（somn.py, __main__.py）✅

# 3. 验证无模块级引擎实例化
grep -rn "= .*Engine\(\)$" src/ --include="*.py"
# 结果: 无结果 ✅

# 4. 验证无模块级可变常量
# 精确扫描: 0个违规 ✅

# 5. 验证核心模块有 __all__
# src/core/、src/utils/、src/intelligence/: 100%覆盖 ✅
```

### 6.2 五项原则合规状态

| 原则 | 要求 | 合规状态 |
|-----|------|---------|
| 1 | 只定义只读全局常量/配置 | ✅ 完全合规 |
| 2 | 仅做依赖导入、全局单例初始化 | ✅ 完全合规 |
| 3 | 业务逻辑封装进函数/类 | ✅ 完全合规 |
| 4 | 统一程序入口 | ✅ 完全合规 |
| 5 | 模块仅做接口导出 | ✅ 核心模块完全合规 |

---

## 七、剩余工作建议

| 项目 | 当前状态 | 建议 |
|-----|---------|------|
| 剩余 129 个文件添加 `__all__` | 83% 覆盖率 | 非核心模块，渐进式补充 |
| None 占位变量优化 | ~100+ 处 | 使用 `@lru_cache` 替代 |
| 统一单例管理模式 | 分散 | 建议制定单例设计规范 |
| `.trash_dir` 残留清理 | 有废弃文件 | 已清理部分，待持续监控 |

---

*本文档由文件整理方案自动生成，整合了代码诊断、修复执行、验证报告的全部内容*