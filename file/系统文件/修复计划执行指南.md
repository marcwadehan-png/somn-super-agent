# 修复计划执行指南

**制定日期**: 2026-04-09
**适用项目**: smart_office_assistant

---

## 一、修复脚本说明

### 1.1 脚本位置
- 主修复脚本: `d:\somn\smart_office_assistant\fix_all_issues.py`
- 详细计划文档: `d:\somn\smart_office_assistant\docs\全量无风险修复计划_20260409.md`

### 1.2 脚本功能

| 功能 | 命令 | 说明 |
|-----|------|------|
| 全量修复 | `python fix_all_issues.py` | 修复全部 878 个问题 |
| 预览模式 | `python fix_all_issues.py --dry-run` | 仅预览，不实际修改 |
| 创建备份 | `python fix_all_issues.py --backup` | 仅创建备份 |
| 回滚 | `python fix_all_issues.py --rollback PATH` | 回滚到指定备份 |
| 验证 | `python fix_all_issues.py --verify` | 仅验证当前状态 |
| 列出备份 | `python fix_all_issues.py --list-backups` | 列出可用备份 |

---

## 二、修复内容概览

### 2.1 问题分布

| 问题类型 | 数量 | 严重程度 | 修复策略 |
|---------|------|---------|---------|
| logging.basicConfig | 2 | 中危 | 自动移除（保留主入口） |
| 可变全局常量 | 254 | 中危 | 惰性初始化模式 |
| 多入口点 | 100 | 高危 | 移动/禁用测试入口 |
| 缺少 __all__ | 522 | 低危 | 自动生成 |

**总计**: 878 个问题

### 2.2 核心修复模式

#### 可变全局变量修复模式

```python
# 修复前
RESULTS = []

# 修复后
_RESULTS: Optional[list] = None

def _get_results() -> list:
    """惰性初始化 getter"""
    global _RESULTS
    if _RESULTS is None:
        _RESULTS = []
    return _RESULTS
```

#### logging.basicConfig 修复模式

```python
# 修复前
import logging
logging.basicConfig(level=logging.INFO)

# 修复后
import logging
logger = logging.getLogger(__name__)  # 移除 basicConfig
```

#### 入口点修复模式

```python
# 修复前
if __name__ == "__main__":
    demo()

# 修复后
if __name__ == "__main__":
    # 已禁用 - 请使用 tests/ 目录
    raise RuntimeError("此入口已禁用")
```

---

## 三、执行步骤

### 3.1 推荐的执行顺序

**Step 0: 验证当前状态**
```bash
cd d:\somn\smart_office_assistant
python fix_all_issues.py --verify
```

**Step 1: 创建备份**
```bash
python fix_all_issues.py --backup
```
> 备份将保存在 `_fix_backups/backup_YYYYMMDD_HHMMSS.zip`

**Step 2: 预览修复**
```bash
python fix_all_issues.py --dry-run
```
> 确认修改内容符合预期

**Step 3: 执行修复**
```bash
python fix_all_issues.py
```
> 输入 `yes` 确认执行

**Step 4: 验证结果**
```bash
python fix_all_issues.py --verify
```

---

## 四、回滚方法

如果修复后出现问题，按以下步骤回滚：

### 4.1 列出可用备份
```bash
python fix_all_issues.py --list-backups
```

### 4.2 回滚到指定备份
```bash
python fix_all_issues.py --rollback _fix_backups/backup_20260409_001200.zip
```

---

## 五、修复验证清单

修复完成后，验证以下指标：

| 检查项 | 目标值 | 检查方法 |
|-------|-------|---------|
| logging.basicConfig 残留 | 0 | 搜索非主入口的 basicConfig |
| 可变全局变量 | <= 50 | 搜索 `= [] / = {} / = set()` |
| 多入口点 | <= 10 | 搜索 `if __name__` |
| __all__ 覆盖率 | >= 80% | 统计有 __all__ 的文件比例 |
| 冒烟测试 | 通过 | `python -c "from src import somn"` |

---

## 六、风险控制

### 6.1 自动备份
- 每次执行修复前自动创建 ZIP 备份
- 备份保留最近 7 天

### 6.2 分步验证
- 每个阶段修复后立即验证
- 验证通过后再进行下一阶段

### 6.3 可逆性保证
- 所有修改都是纯文本替换
- 不改变运行时逻辑
- 可随时回滚到备份

---

## 七、预期结果

修复完成后：

1. **代码规范符合度**: 从当前状态提升到 95% 以上
2. **启动性能**: 略有提升（惰性加载减少预加载开销）
3. **可维护性**: 显著提升（统一的入口点、清晰的导出接口）
4. **风险**: 极低（备份+验证机制确保可回滚）

---

## 八、注意事项

1. **执行时机**: 建议在低峰期或下班后执行
2. **执行时长**: 约 10-30 分钟
3. **备份保留**: 至少保留 7 天
4. **问题反馈**: 如有问题，查看 `fix_all_issues.py` 同目录的 `_code_fix_report.md`
