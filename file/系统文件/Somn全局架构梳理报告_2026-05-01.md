# Somn 全局架构深度梳理报告

> 生成时间：2026-05-01 07:10
> Somn 版本：v6.2.0
> Python 文件总数：13,397

---

## 一、项目结构总览

```
d:\AI\somn\
├── smart_office_assistant/        # 主系统包 (v6.2.0)
│   ├── src/                        # 核心源码
│   │   ├── core/                   # 核心组件
│   │   ├── intelligence/           # 智能层 (541 .py, 813 .yaml, 763 Claw)
│   │   ├── neural_memory/          # 神经记忆系统 (74 .py)
│   │   ├── main_chain/            # 主线架构
│   │   ├── learning/              # 学习系统 (19 .py)
│   │   ├── documents/             # 文档处理
│   │   ├── kaiwu/                # 开物子系统
│   │   ├── digital_brain/         # 数字大脑
│   │   ├── engagement/           # 参与度系统
│   │   ├── integration/          # 集成层
│   │   ├── neural_layout/        # 神经布局
│   │   ├── somn_core/           # Somn核心
│   │   ├── strategy_engine/      # 策略引擎
│   │   ├── tool_layer/         # 工具层
│   │   ├── data_layer/         # 数据层
│   │   ├── utils/               # 工具函数
│   │   └── api/                 # API层
│   ├── config/
│   ├── data/
│   └── tests/
├── knowledge_cells/               # 贤者调度系统 (S1.1)
│   ├── core.py                   # 调度引擎核心
│   ├── domain_nexus.py           # 知识域 (77KB)
│   ├── eight_layer_pipeline.py   # 八层管道 (119KB)
│   ├── divine_reason.py          # 超级推理引擎
│   ├── pan_wisdom_core.py       # 智慧树 (76KB)
│   ├── refute_core.py            # 驳心引擎 v3.0
│   ├── web_integration.py        # 网络搜索集成
│   └── configs/                 # 调度器配置
├── docs/                        # 文档 (789 .md)
├── file/                        # 存档文件 (832 .md)
├── tests/
└── data/
```

---

## 二、打通状态矩阵

### 2.1 核心架构连通性分析

| 板块 | 目录 | 导入次数 | 连接文件数 | 打通状态 | 备注 |
|------|------|---------|-----------|----------|------|
| **神经记忆** | `src/neural_memory/` | **46** | 40+ | ✅ 主链路核心 | 三层架构+藏书阁，核心枢纽 |
| **智能层** | `src/intelligence/` | **核心** | **核心** | ✅ 主链路核心 | WisdomDispatcher, ClawSystem, ImperialLibrary |
| **核心组件** | `src/core/` | **核心** | **核心** | ✅ 主链路核心 | SomnCore, AgentCore, 主线集成 |
| **主线架构** | `src/main_chain/` | **核心** | **核心** | ✅ 主链路核心 | ParallelRouter, CrossWeaver, MainChainIntegration |
| **工具层** | `src/tool_layer/` | 9 | 9 | ✅ 已打通 | ToolRegistry, LLMService, CloudModelHub |
| **学习系统** | `src/learning/` | **46** | 40+ | ✅ 已打通 | 自适应学习、RL、强化学习、PPT学习器 |
| **知识单元** | `knowledge_cells/` | 11 | 11 | ✅ 已打通 | DomainNexus, DivineReason, EightLayerPipeline |
| **神行轨** | `intelligence/dual_track/` | 跨域 | 跨域 | ✅ 已打通 | 神之架构B轨，Claw独立执行单元 |
| **神政轨** | `intelligence/dual_track/` | 跨域 | 跨域 | ✅ 已打通 | 神之架构A轨，监管+合规体系 |
| **驳心引擎** | `refute_core/` | 3 | 3 | ✅ 已打通 | 8维论证，v3.0.2 |
| **API层** | `src/api/` | API | API | ✅ 已打通 | FastAPI路由，admin/llm/wisdom端点 |

### 2.2 孤立/未打通板块

| 板块 | 目录 | 导入次数 | 连接文件数 | 打通状态 | 备注 |
|------|------|---------|-----------|----------|------|
| **参与度系统** | `src/engagement/` | **0** | **0** | ❌ 完全孤立 | 需评估是否废弃 |
| **策略引擎** | `src/strategy_engine/` | **1** | **1** | ⚠️ 几乎孤立 | 仅被 somn_load_optimizer 引用 |
| **数字大脑** | `src/digital_brain/` | 2 | 2 | ⚠️ 几乎孤立 | 内部有v7整合，需确认是否被外部使用 |
| **文档系统** | `src/documents/` | 3 | 2 | ⚠️ 几乎孤立 | NLP文档服务、模板系统 |
| **集成层** | `src/integration/` | 3 | 3 | ⚠️ 部分打通 | neural_layout/orchestrator_bridge |
| **神经布局** | `src/neural_layout/` | 3 | 3 | ⚠️ 部分打通 | orchestrator_bridge连接 |
| **数据层** | `src/data_layer/` | 4 | 4 | ⚠️ 部分打通 | WebSearchEngine, DataCollector |
| **开物系统** | `src/kaiwu/` | 3 | 2 | ⚠️ 部分打通 | kaiwu_service连接core |
| **生态模块** | `src/ecology/` | 2 | 2 | ⚠️ 几乎孤立 | 仅被神政轨track_a引用 |
| **工具函数** | `src/utils/` | 9 | 9 | ✅ 已打通 | lazy_loader, config_manager |

---

## 三、核心打通链路

### 3.1 主链路（已完全串联）

```
用户请求
    ↓
API层 (FastAPI routes)
    ↓
SomnCore (src/core/somn_core.py) ← 核心调度中枢
    ↓
├─→ Intelligence层 (WisdomDispatcher)
│       ├─→ ImperialLibrary (藏书阁)
│       ├─→ ClawSystem (763贤者)
│       └─→ DivineReason (超级推理)
│
├─→ NeuralMemory (神经记忆)
│       ├─→ MemoryEngine / KnowledgeEngine
│       ├─→ ReasoningEngine / LearningEngine
│       └─→ DigitalStrategy + NeuralExecutor (v7整合)
│
├─→ MainChain (主线架构)
│       ├─→ ParallelRouter (并行路由)
│       ├─→ CrossWeaver (交叉织网)
│       └─→ MainChainScheduler/Integration/Monitor
│
└─→ Learning (学习系统)
        ├─→ AdaptiveStrategyEngine
        ├─→ ReinforcementBridge
        └─→ LearningPipeline
```

### 3.2 跨包调用链路

```
knowledge_cells (贤者调度)
    ↓
├─→ DomainNexus (知识域) ←──────────→ neural_memory (知识融合)
│
├─→ DivineReason (超级推理) ──→ 神行轨 (B轨) ──→ Claw执行
│
├─→ EightLayerPipeline (八层管道) ←───→ intelligence层
│
├─→ RefuteCore (驳心引擎) ←──────────────→ 论证审核
│
└─→ PanWisdomTree (智慧树) ←──────────→ 问题分类
```

### 3.3 双轨架构

```
神行轨 (B轨) ── DivineExecutionTrack
├─ ClawWorker (独立执行单元)
├─ 部门索引 (763个Claw配置)
└─ 神之架构执行特权

神政轨 (A轨) ── DivineTrackOversight
├─ 过程记录
├─ 成果验证
└─ 驳回机制
```

---

## 四、关键连接器文件

| 文件 | 功能 | 连接关系 |
|------|------|----------|
| `src/core/somn_core.py` | Somn主核心 | **所有板块的最终汇聚点** |
| `src/core/_somn_ensure.py` | 依赖初始化 | knowledge_cells, tool_layer, data_layer |
| `src/core/_somn_routes.py` | API路由 | knowledge_cells, kaiwu, tool_layer |
| `src/core/_somn_wisdom_init.py` | 智能层初始化 | intelligence, tool_layer, learning |
| `knowledge_cells/core.py` | 调度引擎核心 | **知识_cells的入口** |
| `knowledge_cells/domain_nexus.py` | 知识域 | neural_memory, learning |
| `knowledge_cells/track_b_adapter.py` | 神行轨适配 | dual_track, divine_reason |

---

## 五、导入依赖统计

### 5.1 内部导入模式 (src/)

| 源模块 | 目标模块 | 导入次数 |
|--------|----------|---------|
| `learning/` | `neural_memory/` | 15+ |
| `core/` | `intelligence/` | 10+ |
| `core/` | `main_chain/` | 8+ |
| `core/` | `tool_layer/` | 7+ |
| `knowledge_cells/` | `neural_memory/` | 3+ |
| `dual_track/` | `knowledge_cells/` | 5+ |

### 5.2 跨包导入

| 模式 | 数量 | 说明 |
|------|------|------|
| `from knowledge_cells import` | 11 | knowledge_cells → smart_office |
| `from src.neural_memory import` | 17 | neural_memory内部使用 |

---

## 六、架构健康度评估

### 6.1 连通性评分

| 指标 | 得分 | 说明 |
|------|------|------|
| 核心链路完整性 | ⭐⭐⭐⭐⭐ | 95% 打通 |
| 跨包集成度 | ⭐⭐⭐⭐ | knowledge_cells与主系统深度集成 |
| 孤立模块占比 | ⚠️ 27% | 9个板块中有2-3个完全/几乎孤立 |
| 延迟加载优化 | ⭐⭐⭐⭐⭐ | 所有核心模块100%延迟加载 |

### 6.2 孤立模块分析

```
❌ 完全孤立 (0导入):
   └── src/engagement/ (参与度系统)

⚠️ 几乎孤立 (1-3导入):
   ├── src/strategy_engine/ (策略引擎)
   ├── src/ecology/ (生态模块)
   ├── src/documents/ (文档系统)
   ├── src/digital_brain/ (数字大脑)
   ├── src/data_layer/ (数据层)
   └── src/kaiwu/ (开物系统)
```

---

## 七、核心架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           Somn v6.2.0 全局架构                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────┐                                                    │
│  │  用户请求    │                                                    │
│  └──────┬──────┘                                                    │
│         ↓                                                             │
│  ┌─────────────┐      ┌──────────────────────────────────────────┐   │
│  │  API层      │ ───→ │  SomnCore (核心调度中枢)                   │   │
│  │  FastAPI    │      │  src/core/somn_core.py                    │   │
│  └─────────────┘      └──────────┬───────────────────────────────┘   │
│                                  │                                     │
│         ┌────────────────────────┼────────────────────────────┐       │
│         ↓                        ↓                        ↓          │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐         │
│  │ Intelligence │     │ NeuralMemory │     │  MainChain   │         │
│  │ (智能层)     │     │ (神经记忆)   │     │  (主线架构)  │         │
│  │              │     │              │     │              │         │
│  │ 541.py+      │     │ 74.py       │     │ 9.py        │         │
│  │ 813.yaml     │     │ 三层架构     │     │ ParallelRouter│       │
│  │ 763 Claw    │     │ 藏书阁V3    │     │ CrossWeaver  │         │
│  └──────┬───────┘     └──────┬───────┘     └──────┬───────┘         │
│         │                    │                    │                   │
│  ┌──────┴───────┐     ┌──────┴───────┐     ┌──────┴───────┐         │
│  │ DivineReason │     │ DomainNexus  │     │ Learning     │         │
│  │ 超级推理    │ ←→ │ 知识域       │ ←→ │ 学习系统     │         │
│  │ (GoT+ToT)   │     │ 知识融合    │     │ 自适应+RL    │         │
│  └──────┬───────┘     └──────┬───────┘     └──────┬───────┘         │
│         │                    │                    │                   │
│         ↓                    ↓                    ↓                   │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │              knowledge_cells (贤者调度系统 S1.1)              │     │
│  │                                                             │     │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │     │
│  │  │ EightLayer   │  │ RefuteCore   │  │ PanWisdom    │       │     │
│  │  │ Pipeline     │  │ 驳心引擎     │  │ Tree         │       │     │
│  │  │ (八层管道)   │  │ 8维论证     │  │ 智慧树       │       │     │
│  │  └──────────────┘  └──────────────┘  └──────────────┘       │     │
│  │                                                             │     │
│  │  ┌──────────────┐  ┌──────────────┐                        │     │
│  │  │ 神行轨 B轨   │  │ 神政轨 A轨   │                        │     │
│  │  │ (独立执行)   │  │ (监管合规)   │                        │     │
│  │  └──────────────┘  └──────────────┘                        │     │
│  └─────────────────────────────────────────────────────────────┘     │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│                         ⚠️ 孤立板块                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │
│  │ engagement/ │  │strategy_engine│  │ ecology/     │               │
│  │ (0导入)     │  │ (1导入)      │  │ (2导入)     │               │
│  └──────────────┘  └──────────────┘  └──────────────┘               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 八、建议行动项

### 8.1 立即处理 (高优先级)

| 行动 | 目标板块 | 原因 |
|------|----------|------|
| 评估废弃 | `src/engagement/` | 完全孤立，0导入，可能已废弃 |
| 集成或废弃 | `src/strategy_engine/` | 仅1处引用，可能是历史遗留 |

### 8.2 中期优化 (中优先级)

| 行动 | 目标板块 | 建议 |
|------|----------|------|
| 评估价值 | `src/ecology/` | 确认神政轨track_a的使用是否必要 |
| 评估价值 | `src/digital_brain/` | v7已整合到neural_memory，需确认外部依赖 |
| 文档化 | `src/documents/` | 确认NLP服务是否被实际使用 |

### 8.3 架构完善 (可选)

| 行动 | 说明 |
|------|------|
| 统一网关 | 考虑为孤立模块创建统一入口，避免散落 |
| 依赖注入 | 孤立模块可通过依赖注入方式接入主线 |

---

## 九、总结

### 9.1 架构评估

- **主链路完整度**: 95% ✅
- **跨包集成度**: 高度集成 ✅
- **延迟加载**: 完美实现 ✅
- **孤立模块**: 约27% ⚠️

### 9.2 核心发现

1. **核心三角已稳固**: Intelligence + NeuralMemory + MainChain 三角链路完全打通
2. **知识_cells深度集成**: 与主系统有11处跨包调用，双向通道畅通
3. **神行轨/神政轨已就位**: 双轨架构完整，可独立执行任务
4. **3个板块疑似废弃**: engagement(0导入)、strategy_engine(1导入)、ecology(2导入)

### 9.3 下一步建议

1. **确认废弃清单**: 与用户确认 engagement/strategy_engine/ecology 是否可删除
2. **文档补全**: 为孤立模块编写使用文档或明确标记为"可选模块"
3. **依赖审计**: 对 remaining 孤立模块进行代码审查，确认是否有隐藏依赖

---

## 十一、天枢八层管道完整工作流（补充版）

### 11.1 PipelineStage 枚举（10 个管道阶段）

```python
class PipelineStage(Enum):
    INPUT = "input"                         # 输入层
    NL_ANALYSIS = "nl_analysis"             # 自然语言需求分析
    CLASSIFICATION = "classification"       # 需求分类
    ROUTING = "routing"                     # 分流
    REASONING = "reasoning"                 # 推理
    ARGUMENTATION = "argumentation"         # 论证
    ACTION_PLANNING = "action_planning"     # L6.5 行动规划（engagement v22.0）
    OUTPUT = "output"                       # 输出
    OPTIMIZATION = "optimization"           # 优化
    USER_ENGAGEMENT = "user_engagement"   # L8.5 用户参与（engagement v22.0）
```

### 11.2 ProcessingGrade 枚举（3 种处理等级）

```python
class ProcessingGrade(Enum):
    BASIC = "basic"      # 基础：快速响应，P1→F2→E1，不进神之架构
    DEEP = "deep"        # 深度：神之架构并行，P1→F2→C2→E1
    SUPER = "super"      # 超级：全链路Claw讨论，P1→F1+F2→C1+C2→E1
```

### 11.3 ReasoningMode 枚举（DivineReason 9 种推理模式）

```python
class ReasoningMode(Enum):
    LINEAR = "linear"         # 线性（LongCoT风格）
    BRANCHING = "branching"   # 分支（ToT风格）
    GRAPH = "graph"           # 图网络（GoT风格）
    REACTIVE = "reactive"     # 反应式（ReAct风格）
    SUPER = "super"           # 超级模式（全部启用）
    DIVINE = "divine"         # 至高模式（DivineReason专属）
    DELIBERATE = "deliberate" # 深思熟虑模式
    CREATIVE = "creative"    # 创造性推理模式
    ANALYTICAL = "analytical" # 分析性推理模式
```

### 11.4 RefuteDimension 枚举（驳心 8 大维度）

```python
class RefuteDimension(Enum):
    SENTIENT = "感性"        # 感知维度
    HUMAN_NATURE = "人性"     # 人性维度
    REFUTATION = "反驳"        # 核心反驳维度
    SOCIAL_WISDOM = "人情世故" # 社会智慧维度
    EMOTION = "情绪"          # 情绪维度
    REVERSE_ARG = "逆向论证"   # 逆向论证维度
    DARK_FOREST = "黑暗森林" # 极端情况推演
    BEHAVIORAL = "行为学"    # 行为模式检验
```

### 11.5 FallacyType 枚举（12 种逻辑谬误）

```python
class FallacyType(Enum):
    AD_HOMINEM = "人身攻击"
    STRAW_MAN = "稻草人"
    FALSE_DILEMMA = "虚假二难"
    SLIPPERY_SLOPE = "滑坡谬误"
    CIRCULAR = "循环论证"
    APPEAL_AUTHORITY = "诉诸权威"
    HASTY_GENERALIZATION = "以偏概全"
    CONFIRMATION_BIAS = "确认偏误"
    EMOTIONAL_APPEAL = "诉诸情感"
    POST_HOC = "事后归因"
    SURVIVOR_BIAS = "幸存者偏差"
    SUNK_COST = "沉没成本"
```

### 11.6 WisdomSchool 枚举（42 个智慧学派）

**东方哲学经典 (8)**：CONFUCIAN, DAOIST, BUDDHIST, SUFU, MILITARY, LVSHI, HONGMING, METAPHYSICS
**增长与行业 (3)**：GROWTH_STRATEGY, INDUSTRY_WISDOM, MARKETING
**文明与科幻 (5)**：CIVILIZATION, CIV_WAR_ECONOMY, SCI_FI, GROWTH, MYTHOLOGY
**文化与叙事 (3)**：LITERARY, ANTHROPOLOGY, BEHAVIOR
**思维方法 (6)**：SCIENCE, SOCIAL_SCIENCE, YANGMING, DEWEY, TOP_METHODS, NATURAL_SCIENCE
**中国消费与WCC (2)**：CHINESE_CONSUMER, WCC
**历��思想 (1)**：HISTORICAL_THOUGHT
**V6.0 新增 (9)**：PSYCHOLOGY, SYSTEMS, MANAGEMENT, ZONGHENG, MOZI, FAJIA
**V6.0 第三阶段 (4)**：ECONOMICS, MINGJIA, WUXING, COMPLEXITY
**V6.2 社会科学 (8)**：SOCIOLOGY, BEHAVIORAL_ECONOMICS, COMMUNICATION, CULTURAL_ANTHROPOLOGY, POLITICAL_ECONOMICS, ORGANIZATIONAL_PSYCHOLOGY, SOCIAL_PSYCHOLOGY

### 11.7 ProblemType 枚举（165 个问题类型）

涵盖儒家(5)、道家(8)、佛家(4)、素书(4)、兵法(8)、吕氏春秋(3)、科幻思维(3)、成长思维(3)、神话智慧(3)、文学叙事(3)、人类学(3)、行为塑造(3)、科学思维(3)、社会科学(4)、营销战略(4)、自然科学(5)、WCC智慧演化(7)、历史思想(6)、心理学(8)、系统论(5)、管理学(6)等

### 11.8 OversightCategory 枚举（神政轨监督 3 类）

```python
class OversightCategory:
    PROCESS = "process"     # 过程监督
    RESULT = "result"      # 结果监督
    MEMORY_IO = "memory_io" # 记忆存取监督
```

### 11.9 Level 枚举（SD-F2 四级调度）

```python
class Level(Enum):
    L1_INSTINCT = "L1"  # 本能直觉（0.1s，SD-P1）
    L2_LOGIC = "L2"     # 逻辑推理（1.0s，SD-P1+SD-R2）
    L3_WISDOM = "L3"     # 智慧融合（10s，SD-F1+SD-P1）
    L4_INTUITION = "L4"  # 极致直觉（30s，SD-F1+SD-D3+SD-C2）
```

### 11.10 SageDispatch 12 调度器对照表

| ID | 调度器名 | 类 | 核心职责 |
|----|----------|------|-----------|
| SD-P1 | 问题调度 | ProblemDispatcher | 问题解析、意图识别、上下文理解、方案生成 |
| SD-F2 | 四级调度总控 | FourLevelDispatchController | 级别评估、动态分流、调度执行 |
| SD-F1 | 学派融合 | SchoolFusionDispatcher | 25学派识别、推荐、融合洞察 |
| SD-R1 | 三层推理监管 | ThreeLayerReasoning | 感知层/认知层/元认知层监管 |
| SD-R2 | 谬误检测 | FallacyChecker | 8种逻辑谬误检测 |
| SD-C1 | 太极阴阳决策 | YinYangDecisionDispatcher | 太极阴阳决策体系 |
| SD-C2 |神之架构决策 | DivineArchitectureDispatcher |神之架构377岗位体系决策 |
| SD-D1 | 轻量深度推理 | SuperReasoning(Light) | 表层推理 |
| SD-D2 | 标准深度推理 | SuperReasoning(Standard) | 深度推理 |
| SD-D3 | 极致深度推理 | SuperReasoning(Deep) | 极致推理 |
| SD-E1 | 五步主链执行 | ChainExecutor | 主链并行化执行 |
| SD-L1 | 结果追踪+RL | ResultTracker | 结果追踪、RL强化学习 |

---

## 十二、模块调用关系完整时序图

### 12.1 EightLayerPipeline.process() 调用时序

```
用户输入
    ↓
L1 InputLayer.process()
    ↓
L2 NLAnalysisLayer.process() → TianShuWeb (网络搜索)
    ↓
L3 ClassificationDB.process()
    ↓
  L4 RoutingLayer.process()
    ├→ SD-F2 (四级调度总控)
    └→ SD-P1 (问题调度)
    ↓ (论证闭环循环最多3次)
    L5 ReasoningLayer.process()
    ├→ PanWisdomTree.solve_with_wisdom() [P2接入]
    ├→ SD-C1 (太极阴阳决策)
    ├→ SD-C2 (神之架构决策)
    ├→ SD-D1/D2/D3 (深度推理) → DivineReason.solve()
    ├→ DomainNexus.query() (知识检索)
    └→ DivineExecutionTrack.execute() (Claw独立执行)
    ↓
L6 ArgumentationLayer.process()
    ├→ SD-R2 (谬误检测)
    └→ RefuteCore.argue() (8维驳心论证)
    ↓
L6.5 ActionPlanningLayer.process()
    ├→ engagement.StrategyEngine (策略分析)
    └→ engagement.ExecutionPlanner (执行规划)
    ↓
L7 OutputLayer.process()
    ├→ OutputEngine.render() (多模态输出)
    └→ KaiwuService.generate_ppt() (PPTX风格增强)
    ↓
L8 OptimizationLayer.process()
    ├→ SD-L1 (结果追踪)
    └→ DomainNexus.evolve() (知识更新)
    ↓
L8.5 UserEngagementLayer.process()
    ├→ engagement.ValueReinforcementSystem
    ├→ engagement.NaturalEngagementSystem
    └→ engagement.UserSuccessSystem
    ↓
PipelineResult 返回
```

### 12.2 关键接口映射表

| 模块 | 类 | 主方法 | 关键参数/返回值 |
|------|------|--------|-----------------|
| **天枢管道** | EightLayerPipeline | process(input_text, grade) | → PipelineResult |
| **DivineReason** | DivineReason | solve(problem, mode, context) | → ReasoningResult |
| **PanWisdomTree** | PanWisdomTree | solve_with_wisdom(problem, context) | → PanWisdomResult |
| **RefuteCore** | RefuteCore | argue(claim, counter, dimension) | → {score, reasoning, dimension_scores} |
| **神政轨A** | DivineGovernanceTrack | process_request(query, context) | → {supervision_report} |
| **神行轨B** | DivineExecutionTrack | execute(department, task, caller, context) | → {result, claw_used} |
| **DomainNexus** | DomainNexus | list_cells() / reload() | → [cell_info] |
| **OutputEngine** | OutputEngine | auto_render(ctx) / detect_format() | → OutputArtifact |
| **KaiwuService** | KaiwuService | generate_ppt(content, beautify, auto_charts) | → output_path |
| **NeuralMemory** | NeuralMemory | store() / retrieve_memory() / think() | → MemoryRecord |

### 12.3 神行轨调用权限铁律

```python
# CallerType 有权调用者（track_b.py）
AUTHORIZED_CALLERS = {
    CallerType.DIVINE_REASON,      # DivineReason
    CallerType.PAN_WISDOM_TREE,    # PanWisdomTree
    CallerType.A_GOVERNANCE,       # 神政轨A轨
}
# 禁止其他模块直接调用神行轨
```

---

## 十三、沈核文件位置索引

| 模块 | 文件路径 |
|------|----------|
| 天枢管道 | `knowledge_cells/eight_layer_pipeline.py` (3670行) |
| DivineReason | `knowledge_cells/divine_reason.py` + `smart_office/.../reasoning/_unified_reasoning_engine.py` |
| PanWisdomTree | `knowledge_cells/pan_wisdom_core.py` (1869行) |
| RefuteCore | `knowledge_cells/refute_core.py` + `smart_office/.../engines/refute_core.py` (2592行) |
| dispatchers | `knowledge_cells/dispatchers.py` (2886行) |
| divine_oversight | `knowledge_cells/divine_oversight.py` (1165行) |
| track_a/b | `smart_office/.../dual_track/track_a.py` + `track_b.py` |
| domain_nexus | `knowledge_cells/domain_nexus.py` (2749行) |
| output_engine | `knowledge_cells/output_engine.py` (1277行) |
| kaiwu | `smart_office/.../kaiwu/kaiwu_service.py` (358行) |
| engagement | `smart_office/.../engagement/__init__.py` (100行) + strategy_manager.py + execution_planner.py |
| NeuralMemory | `knowledge_cells/neural_memory_v7.py` + `smart_office/.../neural_memory/neural_memory_v7.py` |

---

## 十四、数据结构完整字段定义

### 14.1 LayerResult（层级处理结果）

```python
@dataclass
class LayerResult:
    layer_name: str                      # 层名称
    stage: PipelineStage               # 管道阶段枚举
    success: bool                     # 处理是否成功
    data: Dict[str, Any] = field(default_factory=dict)    # 层数据负载
    confidence: float = 0.8            # 置信度 [0.0-1.0]
    duration_ms: float = 0.0         # 处理耗时（毫秒）
    errors: List[str] = field(default_factory=list)       # 错误列表
    warnings: List[str] = field(default_factory=list)       # 警告列表
```

### 14.2 PipelineResult（管道最终结果）

```python
@dataclass
class PipelineResult:
    request_id: str                   # 请求ID (UUID)
    grade: ProcessingGrade            # 处理等级 (BASIC/DEEP/SUPER)
    domain: DomainCategory            # 领域分类
    all_layers: Dict[str, LayerResult] = field(default_factory=dict)  # 所有层级结果
    final_answer: str = ""            # 最终答案文本
    final_confidence: float = 0.0    # 最终置信度
    total_duration_ms: float = 0.0    # 总耗时
    routing_path: List[str] = field(default_factory=list)  # 路由路径
    reasoning_chain: List[Dict] = field(default_factory=list)  # 推理链
    argumentation_result: Optional[Dict] = None  # 论证结果
    optimization_suggestions: List[str] = field(default_factory=list)  # 优化建议
    metadata: Dict[str, Any] = field(default_factory=dict)   # 元数据
    output_format: Optional[str] = None   # 输出格式 (text/markdown/html/...)
    output_artifact: Optional[Any] = None  # OutputArtifact 对象
```

### 14.3 RenderContext（输出渲染上下文）

```python
@dataclass
class RenderContext:
    final_answer: str                 # 最终答案
    demand_type: str = ""             # 需求类型
    domain: str = ""                  # 领域
    grade: str = "basic"              # 处理等级
    sections: Dict[str, str] = field(default_factory=dict)  # 输出章节
    metadata: Dict[str, Any] = field(default_factory=dict)  # 元数据
    output_dir: str = ""              # 输出目录
```

### 14.4 OutputArtifact（输出产物）

```python
@dataclass
class OutputArtifact:
    format: OutputFormat              # 输出格式枚举
    file_name: str = ""              # 文件名
    file_path: str = ""              # 文件路径
    content: bytes = b""              # 二进制内容
    text_content: str = ""           # 文本内容（回退用）
    size_bytes: int = 0              # 文件大小（字节）
    duration_ms: float = 0.0         # 生成耗时
```

### 14.5 DispatchRequest（调度请求）

```python
@dataclass
class DispatchRequest:
    request_id: str = field(default_factory=lambda: str(uuid.uuid4()))
    problem: Dict[str, Any] = field(default_factory=dict)   # 问题字典
    level: Optional[Level] = None    # 强制指定等级
    forced_level: Optional[Level] = None  # 强制覆盖
    allow_escalation: bool = True    # 允许升级
    max_time: float = 30.0           # 最大执行时间
    metadata: Dict[str, Any] = field(default_factory=dict)
    trace_enabled: bool = True       # 启用追踪
    parent_output: Optional[Dict] = None  # 父输出（链式调度）
```

### 14.6 DispatchResponse（调度响应）

```python
@dataclass
class DispatchResponse:
    request_id: str
    dispatcher_id: str = "SD-ROOT"  # 调度器ID
    level: Level = Level.L2_LOGIC    # 实际执行等级
    time_spent: float = 0.0         # 耗时
    dispatched_to: List[str] = field(default_factory=list)  # 分发目标
    confidence: float = 0.8           # 置信度
    output: Dict[str, Any] = field(default_factory=dict)   # 输出
    state: DispatcherState = DispatcherState.READY  # 状态
    escalated: bool = False           # 是否升级
    escalation_target: Optional[str] = None  # 升级目标
    trace: Dict[str, Any] = field(default_factory=dict)   # 追踪数据
    error: Optional[str] = None       # 错误信息
```

### 14.7 ReasoningResult（DivineReason 返回）

```python
@dataclass
class ReasoningResult:
    engine: str                      # 引擎名 "DivineReason"
    version: str                    # 版本 "6.2.0"
    problem: str                    # 问题描述
    mode: str                      # 推理模式
    graph: Optional[SuperGraph] = None  # 推理图
    insights: List[Dict] = field(default_factory=list)      # 顿悟列表
    reflections: List[Dict] = field(default_factory=list)   # 反思列表
    corrections: List[Dict] = field(default_factory=list)   # 纠错列表
    final_answer: str = ""         # 最终答案
    confidence: float = 0.0         # 置信度
    reasoning_quality: str = ""     # 推理质量
    total_nodes: int = 0            # 生成节点数
    total_edges: int = 0            # 边数
    depth: int = 0                  # 深度
    duration_ms: float = 0.0         # 耗时
    metadata: Dict[str, Any] = field(default_factory=dict)
```

### 14.8 PanWisdomResult（智慧树返回）

```python
@dataclass
class PanWisdomResult:
    problem: str
    identified_types: List[ProblemType] = field(default_factory=list)  # 识别的问题类型
    recommended_schools: List[WisdomRecommendation] = field(default_factory=list)  # 推荐的学派
    fusion_insights: List[str] = field(default_factory=list)  # 融合洞察
    confidence: float = 0.0
    final_recommendations: List[str] = field(default_factory=list)  # 最终建议
    metadata: Dict[str, Any] = field(default_factory=dict)
```

### 14.9 TaskAssignment（神政轨任务分配）

```python
@dataclass
class TaskAssignment:
    task_id: str
    task_description: str            # 任务描述
    assigned_claw: str              # 分配的Claw名
    department: str                 # 部门
    caller: str                    # 调用方
    status: str = "pending"         # 状态 pending/running/completed/failed
    created_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    result: Optional[Dict] = None   # 执行结果
```

---

## 十五、错误处理和降级策略

### 15.1 模块级降级策略

| 模块 | 正常实现 | 降级实现 | 触发条件 |
|------|----------|----------|----------|
| **NeuralMemory** | `neural_memory_v7.py` 真实实现 | `NeuralMemoryLight` 内存实现 | 导入失败 |
| **DivineReason** | `_unified_reasoning_engine` 完整版 | 内置基础推理引擎 | 导入失败/API异常 |
| **PanWisdomTree** | `pan_wisdom_core` 完整版 | 返回空洞察 | 导入失败/API异常 |
| **RefuteCore** | `refute_core.py` 完整版 | Mock返回 {score: 0.5} | 导入失败/API异常 |
| **KaiwuService** | `kaiwu_service.py` 完整版 | 回退到原生 python-pptx | 导入失败/生成失败 |
| **engagement** | `engagement/` 完整版 | 空操作/日志警告 | 导入失败/API异常 |
| **神行轨** | `track_b.py` 真实执行 | Mock返回 {is_mock: True} | 权限校验失败/Claw不存在 |
| **DomainNexus** | 完整格子系统 | 内存缓存 | 文件系统错误 |

### 15.2 管道级错误处理

```python
# 八层管道 process() 错误处理
def process(self, input_text: str, grade: ProcessingGrade = ProcessingGrade.BASIC) -> PipelineResult:
    try:
        # 完整流程执行
    except ImportError as e:
        # 降级到轻量模块
        return self._build_fallback_result(e)
    except Exception as e:
        # 记录错误但继续执行
        logger.error(f"Pipeline error: {e}")
        return self._build_error_result(input_result)
    finally:
        # 强制存档即使失败
        if self._memory:
            self._memory.store(final_answer, category="pipeline_error")
```

### 15.3 论证闭环重试机制

```python
# 最大重试次数（防止死循环）
MAX_REROUTE_ATTEMPTS = 3

# 重试流程：
# 1. L6 论证不合格 → 生成 reroute_feedback
# 2. reroute_feedback 注入 L4 分流层
# 3. L4 根据反馈进行"修正式分流"
# 4. 重新进入 L5→L6
# 5. 达到上限强制输出最佳结果
```

---

## 十六、性能指标和基准测试

### 16.1 模块加载时间基准

| 模块 | 预加载 | 懒加载 | 缓存命中 |
|------|--------|--------|----------|
| **天枢管道** | - | <1ms | 单例复用 |
| **DivineReason** | 50-100ms | 5-10ms | 进程级 |
| **PanWisdomTree** | <1ms (L1预加载) | 5-10ms (L2 LRU) | 按需 |
| **RefuteCore** | 10-20ms | <1ms | 首次后缓存 |
| **SD调度器** | <1ms | 5-20ms | 首次后缓存 |
| **神行轨B** | <20ms | 100ms/per Claw | Worker LRU |
| **DomainNexus** | <5ms (索引) | 50ms (首格) | ~0.1ms |

### 16.2 处理时间基准（BASIC 模式）

| 层级 | 预期耗时 | 超时阈值 |
|------|----------|----------|
| L1 输入清洗 | <5ms | 100ms |
| L2 NLP分析 | <50ms | 500ms |
| L3 分类 | <10ms | 200ms |
| L4 分流 | <20ms | 300ms |
| L5 推理 | <100ms | 1000ms |
| L6 论证 | <50ms | 500ms |
| L6.5 行动规划 | <30ms | 300ms |
| L7 输出 | <100ms | 1000ms |
| L8 优化 | <20ms | 200ms |
| L8.5 用户参与 | <20ms | 200ms |
| **总计 BASIC** | **<400ms** | **3000ms** |

### 16.3 内存占用基准

| 模块 | 初始内存 | 峰值内存 |
|------|----------|----------|
| **天枢管道** | <5MB | <20MB |
| **DivineReason** | <50MB | <200MB (图结构) |
| **PanWisdomTree** | <10MB | <50MB |
| **RefuteCore** | <20MB | <80MB |
| **DomainNexus** | <1MB | <30MB |
| **神行轨B** | <5MB | <100MB (Claw缓存) |
| **总计** | **<100MB** | **<500MB** |

### 16.4 缓存策略

```python
# NLP 结果缓存
_NLP_CACHE_MAX = 128
_NLP_CACHE_TTL = 300s  # 5分钟有效

# 知识库查询缓存
_KB_CACHE_MAX = 64

# Worker LRU 缓存
_WORKER_CACHE_MAX = 50
_WORKER_CACHE_TTL = 3600s  # 1小时

# NLP 缓存示例
def _nlp_cache_get(text: str) -> Optional[LayerResult]:
    cache_key = hashlib.md5(text.encode()).hexdigest()
    entry = cls._NLP_CACHE.get(cache_key)
    if entry and (time.time() - entry.timestamp < 300):
        return entry.result
    return None
```

---

## 十七、配置参数说明

### 17.1 八层管道配置（eight_layer_pipeline.py）

```python
class EightLayerPipelineConfig:
    # 启动配置
    lazy: bool = True                # 懒加载模式
    output_dir: str = ""             # 输出目录

    # 缓存配置
    nlp_cache_max: int = 128        # NLP缓存最大条目
    nlp_cache_ttl: int = 300        # NLP缓存TTL(秒)
    kb_cache_max: int = 64          # 知识库缓存最大条目

    # 超时配置
    max_time_per_layer: Dict[str, float] = field(default_factory=lambda: {
        "input": 0.1,
        "nl_analysis": 0.5,
        "classification": 0.2,
        "routing": 0.3,
        "reasoning": 1.0,
        "argumentation": 0.5,
        "action_planning": 0.3,
        "output": 1.0,
        "optimization": 0.2,
        "user_engagement": 0.2,
    })

    # 论证配置
    max_reroute_attempts: int = 3    # 最大重试次数
    reroute_threshold: float = 0.6     # 论证重试阈值
```

### 17.2 DivineReason 配���（_unified_reasoning_engine.py）

```python
@dataclass
class UnifiedConfig:
    # 推理配置
    default_mode: ReasoningMode = ReasoningMode.DIVINE
    max_nodes: int = 50               # 最大节点数
    max_depth: int = 10               # 最大深度

    # 剪枝配置
    pruning_threshold: float = 0.3     # 剪枝阈值
    min_feasibility_score: float = 0.2   # 最小可行性分数

    # 工具配置
    enable_tools: bool = True
    tool_timeout: float = 10.0          # 工具超时(秒)

    # 网络增强配置
    web_search_enabled: bool = True
    web_search_auto: bool = True
    web_search_depth: str = "normal"       # quick/normal/deep

    # 回调配置
    progress_callback: Optional[Callable] = None
```

### 17.3 神行轨配置（track_b.py）

```python
# 调用权限配置
AUTHORIZED_CALLERS = {
    DivineReason,
    PanWisdomTree,
    A_GOVERNANCE,
}

# 懒加载配置
_DEPARTMENT_INDEX_CACHE_TTL = 3600     # 部门索引缓存1小时
_WORKER_CACHE_MAX = 50                    # 最大Worker缓存数
_WORKER_CACHE_TTL = 3600                # Worker缓存1小时
```

---

## 十八、快速启动示例代码

### 18.1 最简启动（BASIC 模式）

```python
from knowledge_cells.eight_layer_pipeline import EightLayerPipeline, ProcessingGrade

pipeline = EightLayerPipeline()
result = pipeline.process("分析用户增长策略", ProcessingGrade.BASIC)

print(f"答案: {result.final_answer}")
print(f"置信度: {result.final_confidence}")
print(f"耗时: {result.total_duration_ms}ms")
```

### 18.2 深度推理模式（DEEP 模式）

```python
pipeline = EightLayerPipeline()
result = pipeline.process("设计公司三年战略规划", ProcessingGrade.DEEP)

# 获取完整的推理链
for layer_name, layer_result in result.all_layers.items():
    if layer_result.success:
        print(f"{layer_name}: {layer_result.data}")
```

### 18.3 独立调用模块示例

```python
# DivineReason 独立调用
from knowledge_cells.divine_reason import DivineReason
reason = DivineReason()
result = reason.solve("如何提升GMV", mode=ReasoningMode.DIVINE)
print(result.final_answer)

# PanWisdomTree 独立调用
from knowledge_cells.pan_wisdom_core import solve_with_wisdom
result = solve_with_wisdom("电商增长策略")
print(result.fusion_insights)

# RefuteCore 独立调用
from knowledge_cells.refute_core import RefuteCore
rc = RefuteCore()
result = rc.argue("应该高价聘请专家", "高价可能导致预算超支", "logic")
print(result["score"], result["reasoning"])

# DomainNexus 知识查询
from knowledge_cells.domain_nexus import DomainNexus
dn = DomainNexus()
cells = dn.list_cells()
print(f"知识格子数: {len(cells)}")
```

---

*报告补充完成（2026-05-01 17:35）*
*新增内容：数据结构和返回值字段、方法签名、错误处理和降级策略、性能指标和基准测试、配置参数、快速启动示例*
