# Somn 项目清理扫描报告

> **扫描时间**: 2026-04-29 15:40  
> **扫描范围**: `d:\AI\somn\` 全目录（940+ 文件）  
> **策略**: 只扫描不删除，报告可清理项

---

## 总览

| 类别 | 数量 | 风险等级 | 建议 |
|------|------|---------|------|
| 🔴 IDE/工具缓存 | 139 文件 | 低 | 加入 .gitignore |
| 🟡 空目录 | 9 个 | 低 | 删除或加 .gitkeep |
| 🟡 根目录散落文件 | 12 个 | 中 | 整理归位 |
| 🟡 历史版本文件 | 11 个 | 中 | 确认后删除 |
| 🟡 测试/临时产物 | 10 个 | 中 | 清理 |
| 🟢 运行时数据 | 29 个 | 低 | 保留（运行时生成） |
| 🟢 Node.js 残留 | 3 个 | 低 | 确认后清理 |

---

## 一、IDE/工具缓存（139 文件）🔴

### 1.1 `.pytest_cache/`（7 文件）
```
d:\AI\somn\.pytest_cache\
```
pytest 运行时缓存，应加入 `.gitignore`。

### 1.2 `.workbuddy/`（132 文件）
```
d:\AI\somn\.workbuddy\
├── expert-history.json       (2.59 KB)
├── settings.local.json       (76 B)
├── memory/                   (30 个 md 文件，含 15 个空文件)
├── teams/                    (10 个团队目录，含 config.json + inbox JSON)
└── temp_persona/             (docx 解压临时文件，18 个 XML 文件)
```
WorkBuddy IDE 工作数据，必须加入 `.gitignore`（不出现在开源仓库中）。

**子项详情**：
- `teams/` 下 10 个团队（600-sages-batch2/batch4、sage-deep-learning-team、6 个 _auto_*），含 64 个 JSON
- `memory/` 下 30 个 md（15 个为 0B 空文件）
- `temp_persona/unpacked/` 含 docx 临时解压文件（约 113 KB）

---

## 二、空目录（9 个）🟡

| 路径 | 用途推测 |
|------|---------|
| `smart_office_assistant/data/core/` | 核心数据（未使用） |
| `smart_office_assistant/data/cloud_hub/` | 云端数据（未使用） |
| `smart_office_assistant/data/rag_index/` | RAG 索引（未使用） |
| `smart_office_assistant/data/library_upgrade/` | 藏书阁升级（未使用） |
| `smart_office_assistant/data/memory/research_framework/knowledge_base/` | 知识库（空） |
| `smart_office_assistant/data/teacher_student/data/` | 导师学生数据（空） |
| `smart_office_assistant/data/teacher_student/data/teacher_student/` | 导师学生数据（空） |
| `.workbuddy/temp_persona/unpacked/word/theme/` | docx 临时（空） |
| `.workbuddy/temp_persona/unpacked/word/_rels/` | docx 临时（空） |

**建议**: 删除空目录，或在需要保留结构的目录中放 `.gitkeep`。

---

## 三、根目录散落文件（12 个）🟡

| 文件 | 大小 | 建议 |
|------|------|------|
| `SomnApp.py` | 20.31 KB | 移入包目录（保留为入口） |
| `start_server.py` | 3.89 KB | 保留为 API 入口，或移入 `scripts/` |
| `start_independent.py` | 10.37 KB | 保留为独立运行入口 |
| `start_independent.bat` | 3.46 KB | 保留（与 .sh 配套） |
| `start_independent.sh` | 3.39 KB | 保留（与 .bat 配套） |
| `run_somn.bat` | 186 B | ⚠️ 与 `smart_office_assistant/start_somn.bat` 功能重复，建议删除 |
| `quick_run.py` | 525 B | 移入 `tests/` 或删除 |
| `divine_reason_ab_test_results.json` | — | 测试输出，应删除或移入 `tests/` |
| `package-lock.json` | 7.72 KB | ⚠️ 非核心依赖，确认后删除 |
| `.markdownlinkcheck.json` | 261 B | 移入 `.github/` 或保留 |
| `.markdownlint.json` | 455 B | 移入 `.github/` 或保留 |

---

## 四、历史版本/重复文件（11 个）🟡

### 4.1 带版本号的历史 Python 文件

| 文件 | 大小 | 现行文件 | 建议 |
|------|------|---------|------|
| `tests/neural_memory/test_neural_system_v1.py` | — | `test_neural_memory_v2.py` | 确认后删除 |
| `tests/test_documents_v22.py` | 9.28 KB | — | 确认版本后删除 |
| `tests/test_memory_engine_v2.py` | 21.08 KB | — | 统一到最新版 |
| `tests/test_neural_memory_v2.py` | 18.05 KB | — | 统一到最新版 |
| `src/neural_memory/memory_engine_v2.py` | 15.36 KB | `neural_memory_v2.py` | 检查是否冗余 |
| `src/neural_memory/neural_memory_v2.py` | 37.58 KB | — | 检查是否为当前版本 |
| `src/neural_memory/memory_system_v3.py` (推测) | — | — | 版本统一检查 |
| `src/documents/test_integration_v22.py` | 17.03 KB | — | 移入 `tests/` 或删除 |
| `src/industry_engine/industry_profiles/_ip_v1.py` | — | — | 检查是否仍被引用 |
| `src/intelligence/engines/dao_wisdom/_dao_v2.py` | — | — | 检查是否仍被引用 |

### 4.2 历史版本目录

| 目录 | 建议 |
|------|------|
| `smart_office_assistant/data/memory/memory_v2/` | 检查是否仍被使用 |

### 4.3 项目进度历史文件

| 文件 | 大小 |
|------|------|
| `docs/项目进度/p0_upgrade_report.json` | 452 B |
| `docs/项目进度/p0_upgrade_v2_report.json` | 268 B |
| `docs/项目进度/p0_upgrade_v3_report.json` | 283 B |

**建议**: v3 是最终版，v1/v2 可删除。

---

## 五、测试/临时产物（10 个）🟡

| 文件/目录 | 大小 | 说明 |
|----------|------|------|
| `smart_office_assistant/src/documents/test_output/test_document.docx` | — | 测试生成 |
| `smart_office_assistant/src/documents/test_output/test_workbook.xlsx` | — | 测试生成 |
| `knowledge_cells/.cell_index_cache.pkl` | 2.69 KB | 索引缓存 |
| `smart_office_assistant/data/engine_cache/wisdom_engines.pkl` | 158.3 KB | 引擎缓存 |
| `smart_office_assistant/src/growth_engine/.trash` | 48.22 KB | 回收站文件 |
| `divine_reason_ab_test_results.json` | — | 测试输出 |
| `docs/项目清理扫描报告_20260429.md`（本文件旧版） | 7.55 KB | 已被新版替代 |

### 其他临时/特殊文件

| 文件 | 大小 | 说明 |
|------|------|------|
| `smart_office_assistant/SmartOffice.spec` | 2.23 KB | PyInstaller 打包配置 |
| `docs/核心_独立运行需求.docm` | 10.79 KB | Word 宏文档 |
| `smart_office_assistant/package.json` | 54 B | Node.js 包配置 |
| `smart_office_assistant/package-lock.json` | 6.55 KB | Node.js 锁文件 |
| `smart_office_assistant/generate_mengniu_ppt.js` | 14.53 KB | PPT 生成脚本 |

---

## 六、运行时数据（保留）🟢

以下文件为程序运行时自动生成，不需要清理，但应确保在 `.gitignore` 中：

### 6.1 SQLite 数据库（4 组）

| 数据库 | 主文件 | WAL | SHM |
|--------|-------|-----|-----|
| 知识库 | `data/knowledge/knowledge.db` (48 KB) | — | — |
| 日志 | `data/logs/logs.db` (4 KB) | `logs.db-wal` (84.5 KB) | `logs.db-shm` (32 KB) |
| 记忆 | `data/memory/memory.db` (56 KB) | — | — |
| 运行 | `data/run/run.db` (4 KB) | `run.db-wal` (80.5 KB) | `run.db-shm` (32 KB) |

### 6.2 运行时 JSON 数据（28 个）

| 文件 | 大小 | 用途 |
|------|------|------|
| `data/reasoning_memory.json` | 75.01 KB | 推理记忆 |
| `data/autonomy/experience_library.json` | 37.53 KB | 经验库 |
| `data/autonomy/task_reflections.json` | 41.76 KB | 任务反思 |
| `data/claws/严助/memory/connections.json` | 42.08 KB | Claw 连接 |
| `data/claws/丁肇中/memory/connections.json` | 33.75 KB | Claw 连接 |
| `data/claws/亚当·斯密/memory/connections.json` | 21.01 KB | Claw 连接 |
| 其他 22 个 | <20 KB | 配置/缓存/索引 |

### 6.3 藏书阁 YAML 数据

- `data/imperial_library/LIB_00001~00005.yaml`（5 个模板文件）
- `data/imperial_library/library_index.json`
- `data/imperial_library/乙/`（2 个 UUID JSON）

---

## 七、项目健康检查 ✅

以下项目均**干净**，无需处理：

| 检查项 | 状态 |
|--------|------|
| `__pycache__/` 目录 | ✅ 无 |
| `.pyc` 文件 | ✅ 无 |
| `.idea/` / `.vscode/` | ✅ 无 |
| `.egg-info/` | ✅ 无 |
| `dist/` / `build/` | ✅ 无 |
| `node_modules/` | ✅ 无 |
| `.log` 文件 | ✅ 无 |
| `.bak` / `.tmp` / `.swp` | ✅ 无 |
| `.DS_Store` / `Thumbs.db` | ✅ 无 |
| `.cache*` 文件 | ✅ 无 |
| 大文件 (>1MB) | ✅ 无（最大 sage_codes.json 860 KB） |

---

## 八、.gitignore 建议补充

以下条目建议添加到 `.gitignore`：

```gitignore
# IDE / Tool Cache
.pytest_cache/
.workbuddy/

# Runtime Data
*.pkl
*.db
*.db-shm
*.db-wal
smart_office_assistant/data/

# Test Outputs
smart_office_assistant/src/documents/test_output/
divine_reason_ab_test_results.json

# Node.js (if not used)
package-lock.json
smart_office_assistant/package-lock.json
smart_office_assistant/node_modules/
```

---

## 九、优先清理建议

### 高优先（安全、低风险）
1. ✅ `.pytest_cache/` — pytest 缓存
2. ✅ `.workbuddy/` — IDE 工具数据（136 文件）
3. ✅ 9 个空目录
4. ✅ `run_somn.bat`（根目录，与包内重复）
5. ✅ `quick_run.py`（移入 tests/）
6. ✅ `knowledge_cells/.cell_index_cache.pkl`

### 中优先（需确认）
1. ⚠️ 11 个历史版本文件 — 需确认是否有引用
2. ⚠️ `p0_upgrade_v1_report.json` / `p0_upgrade_v2_report.json` — 保留 v3
3. ⚠️ Node.js 残留（`package*.json` × 3）— 确认 generate_mengniu_ppt.js 是否仍需要

### 低优先（维护性质）
1. `.markdownlint.json` / `.markdownlinkcheck.json` 移入 `.github/`
2. `src/growth_engine/.trash` 清理
3. `src/documents/test_integration_v22.py` 移入 tests/

---

*报告由 C0dy 自动生成 — 2026-04-29*
